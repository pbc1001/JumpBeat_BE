# JumpBeat 요구사항

## 1. 서비스 정의

JumpBeat는 YouTube 영상을 재생하면서 노래 가사를 제한 시간 안에 입력하는 리듬형 타자 연습 서비스다.

사용자는 다음 두 경로로 서비스를 이용한다.

1. 공개된 기존 곡을 검색하고 바로 플레이한다.
2. 제목, 가수, 가사, YouTube URL을 입력하고 가사 싱크를 직접 지정해 새로운 곡을 공개한다.

## 2. MVP 범위

### 2.1 회원

- 이메일, 닉네임, 비밀번호로 가입한다.
- 이메일과 비밀번호로 로그인한다.
- 이메일과 닉네임은 각각 중복될 수 없다.
- 로그인한 사용자는 곡 등록, 결과 저장, 자신의 곡 수정·삭제를 할 수 있다.
- 비로그인 사용자의 곡 목록 조회 허용 여부는 구현 시 결정하되, 결과 저장은 로그인 사용자만 가능하다.
- 소셜 로그인과 비밀번호 찾기는 1차 MVP에서 제외한다.

### 2.2 공개 곡 탐색

- 공개된 곡을 최신순 또는 인기순으로 조회한다.
- 제목이나 가수명으로 검색한다.
- 난이도와 언어로 필터링할 수 있다.
- 곡 상세에서 제목, 가수, 등록자, YouTube 영상, 가사 수, 플레이 수를 확인한다.

### 2.3 곡 등록

입력 항목:

- 노래 제목: 필수
- 가수명: 필수
- YouTube URL: 필수
- 가사: 필수, 줄바꿈을 기준으로 가사 줄을 나눈다.
- 언어: 필수
- 난이도: 필수

등록 흐름:

1. YouTube URL에서 `videoId`를 추출한다.
2. 영상이 공식 임베드 플레이어에서 재생되는지 사용자가 미리보기로 확인한다.
3. 제목과 가수명이 유사한 공개 곡을 조회한다.
4. 중복 후보가 있으면 기존 곡 이용 또는 계속 등록을 선택하는 모달을 표시한다.
5. 계속 등록을 선택하면 비공개 초안을 만든다.
6. YouTube 영상을 재생하며 각 가사의 시작 순간에 단축키를 눌러 시간을 기록한다.
7. 싱크 미리보기와 유효성 검사를 통과한 뒤 최종 등록한다.
8. 최종 등록된 곡은 즉시 모든 사용자에게 공개된다.

중복은 경고 대상일 뿐 등록 차단 조건이 아니다. 제목과 가수명을 공백·대소문자·일부 특수문자를 정규화하여 비교하되, 최종 선택은 사용자에게 맡긴다.

### 2.4 싱크 편집

- 각 가사 줄에는 시작 시간 `startTimeMs`만 저장한다.
- 시작 시간은 0 이상의 정수 밀리초다.
- 시작 시간은 가사 순서에 따라 반드시 증가해야 한다.
- 사용자가 단축키를 누르면 YouTube Player의 현재 시간을 기록하고 다음 줄로 이동한다.
- 개별 시간을 직접 수정하거나 다시 기록할 수 있다.
- 전체 가사에 공통으로 적용하는 싱크 보정값은 편집 편의 기능으로 제공할 수 있다.
- 모든 줄의 시간이 지정되어야 최종 공개할 수 있다.
- 마지막 가사의 마감 시간은 영상 종료 시간이다.

YouTube 영상이나 음원을 서버가 다운로드하지 않는다. 영상 재생과 현재 시간 확인에는 공식 YouTube IFrame Player API를 사용한다.

### 2.5 타자 게임

공통 규칙:

- 현재 가사의 입력 가능 구간은 해당 가사의 `startTimeMs`부터 다음 가사의 `startTimeMs` 직전까지다.
- 마지막 가사는 영상 종료 전까지 입력할 수 있다.
- 사용자가 Enter를 누른 순간 정답을 판정한다.
- 입력 가능 시간 전에는 제출할 수 없다.
- 한 가사당 한 번만 제출할 수 있다.
- 정답 제출은 100점, 오답 제출과 시간 초과는 0점이다.
- 제출하지 않은 상태에서 다음 가사 시간이 되면 `Miss`를 표시하고 자동으로 다음 가사로 넘어간다.
- 영상이 일시정지되거나 버퍼링되면 게임 타이머도 YouTube 재생 시간과 함께 멈춘다.

문자열 비교 전 다음 정규화를 적용한다.

- 앞뒤 공백 제거
- 연속 공백을 한 칸으로 변환
- Unicode NFC 정규화
- 영문 대소문자 무시
- 쉼표, 마침표, 느낌표, 물음표 등 일반 문장부호 무시

게임 모드:

- `CONTINUE`: 오답이나 시간 초과가 발생해도 Miss 처리 후 다음 가사로 진행한다.
- `SURVIVAL`: 첫 오답 제출 또는 시간 초과 시 즉시 종료한다.

### 2.6 결과와 개인 기록

- 총점: 정답 가사 수 × 100
- 정확도: 정답 가사 수 ÷ 판정 대상 전체 가사 수 × 100
- Miss 수: 오답 제출 수 + 시간 초과 수
- 최대 콤보: 연속 정답의 최댓값
- 완주율: 판정 완료 가사 수 ÷ 전체 가사 수 × 100
- 결과에 모드, 시작·종료 시각, 플레이 시간, 정답 수, Miss 수를 저장한다.
- 사용자는 자신의 플레이 기록을 최신순으로 조회할 수 있다.
- 다른 사용자의 점수 비교와 전체·곡별 랭킹은 제공하지 않는다.

## 3. 예외 처리

- 잘못된 YouTube URL이면 등록을 진행하지 않는다.
- 삭제·비공개·연령 제한·지역 제한·임베드 금지 영상은 재생 불가 상태를 안내한다.
- 공개 후 영상이 재생 불가능해지면 곡을 `UNAVAILABLE` 상태로 전환할 수 있다.
- 원본 영상과 가사는 등록자가 수정할 수 있으며, 이미 저장된 과거 결과는 유지한다.
- 곡 삭제는 우선 소프트 삭제하여 기록의 참조 무결성을 보존한다.

## 4. 운영 필수 기능

모든 곡이 즉시 공개되므로 다음 기능을 운영 범위에 포함한다.

- 곡 등록자와 등록·수정 시간 기록
- 콘텐츠 신고 접수
- 관리자 곡 숨김 처리
- 이용약관 및 저작권 침해 신고 안내
- 악성 입력 방지를 위한 길이 제한, 검증, 요청 횟수 제한

신고 처리와 관리자 화면은 초기 플레이 기능 이후 구현할 수 있지만 데이터 구조는 처음부터 고려한다.

## 5. MVP 제외 항목

- YouTube 영상 또는 음원 다운로드
- AI 자동 싱크
- 실시간 멀티플레이
- 소셜 로그인
- 모바일 앱
- 결제 및 유료 콘텐츠
